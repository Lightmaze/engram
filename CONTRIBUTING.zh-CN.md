# 参与贡献

改动应服务于 v0 目标：原始上下文全文、具名召唤、多轮陪伴、观察、休眠、发言归属、带来源的 Outcome 驱动自我折叠、MCP 可移植性和可选 Hook 守护。

结果驱动的自我折叠只有在同时满足以下边界时才属于本仓库：原始消息不变、证据作用域明确、作者属于 Engram、历史只追加、当前姿态可回退，并且不制造持续 Human Review（人工终审）债务。改变只是 Engram 的 `posture/hypothesis`（姿态／假设），不是经过用户追认的事实。来源位置、角色标签和内容摘要都不能被描述成外部现实的独立证明。

不要把审批队列、用户评分、标量奖励、定时或无来源成长、代际治理机械、合成用户评测或实验档案加入 v0 最小内核。除非具体生活失败证明内核确有需要，更大的系统应另行讨论。

涉及 self-fold 的改动至少要验证有关不变量：原始消息逐字节不变；不合格证据不能制造成长；`no_change` 不移动当前姿态；模型失败会保留 Outcome 供幂等重试；回退恢复父姿态且不删除历史。

提交合并请求前运行：

```powershell
go test ./...
go vet ./...
Set-Location integrations/pi-engram
pnpm install --frozen-lockfile
pnpm check
pnpm test
```

公共契约变化时，应同时更新英文和简体中文两套文档。
