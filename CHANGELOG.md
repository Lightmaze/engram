# 变更记录

## v0.3.0

- 新增 `wake_event_id` 与 `observation_event_id`，并增加 `engram_outcome`、`engram_fold_status`、`engram_fold_revert`，MCP 公开面扩展为十个工具。
- 调用者可以把后来记录的用户消息、工具结果或另行标明来源的外部观察，关联到一次具体唤醒。来源种类和本地事件链检查只限制记录的含义；`source_digest` 只是收到内容字节的 SHA-256，不是身份认证或外部事实证明。
- Outcome 持久保存后，Engram 会立即自行作出 `change`（改变）或 `no_change`（不改变）。改变会成为 Engram 自写、权限仅为 `posture/hypothesis`（姿态／假设）的当前姿态，不进入人工审批队列；`no_change` 不移动原有姿态。
- 原始消息始终不变。以后唤醒会在明确的 Engram 作者边界之后装载当前 Fold，`active_fold_event_id` 使本次使用的姿态可以追踪。
- Fold 模型失败时保留 Outcome，并允许使用同一 `request_id` 幂等重试。回退通过追加纠正事件恢复父姿态，不删除 Outcome 或 Fold 历史。
- 历史 Fold、`no_change` 和回退理由都会作为 Engram 经历重新装载，只有最后标明的有效 Fold 才是当前姿态。唤醒模型返回空文本仍是合法沉默；Fold 返回空文本则仍然报错。
- 增加只追加的 Journal 格式护栏，使 v0.1／v0.2 二进制面对升级数据时明确失败，而不是静默忽略 v0.3 成长事件；未知事件种类同样 fail-closed（拒绝继续）。
- 模型默认输出上限提高到 4096 token，并真正传给 DeepSeek 兼容接口；随仓库提供的 guardian 唤醒超时也与 120 秒模型上限对齐。
- Pi 适配器扩展为七个显式工具。Pi 和其他 Hook 自动守护仍只保存观察，不会自行把观察晋升为 Outcome，也不会自动触发 self-fold。

## v0.2.0

- Pi 自动守护唤醒现在提交真实活跃分支的一段有序对话，不再只提交最新提示。
- 第一次唤醒包含可见的用户与主 Agent 消息、工具调用和工具结果；工具调用编号使并行调用能够对应各自结果，以后再通过持久的私有游标只提交新增分支。
- 先前的 Engram 注入、隐藏思考、图片数据和无法表达的消息种类会被排除并明确报告；64 KiB UTF-8 上限会标明省略前缀或截断。
- Pi 会把主 Agent 消息、工具结果和同一次运行中排队的用户中途引导或后续消息按原角色写回为外部观察，并避免重复写入已经位于唤醒现场中的首条提示。
- 守护索引或观察无法持久保存时，Pi 现在能明确得知失败；主 Agent 仍可继续，但游标不会推进，遗漏回合会在下次重放。
- CI 现在会为 Pi 生命周期测试构建真实 Go 二进制，跨运行时测试不再通过跳过而变绿。
- 新增分开的中英文开发宪章，固定项目目的：完整 Engram 上下文、真实主线程切片、有界陪伴、发言归属，以及不把持续 Human Review 债务转嫁给用户。

## v0.1.3

- 为每次 Engram 唤醒增加由运行时生成的 `attribution`，包含 Engram 编号、名称、可选历史立场和陪伴期编号；它是发言归属，不是密码学签名。
- MCP 结构化结果、直接 Hook 注入、Cursor 待取回结果与 Pi 消息详情现在都保留同一份归属。
- 文本型宿主收到带单行 JSON 归属信息的可见封套，避免 Engram 发言在注入后被误记为用户或主 Agent 的话。
- `statement` 仍不进入模型提供方的身份提示；归属封套只服务宿主显示与历史保存。
- 在 Codex Hook 的 5000 字节上限内完整保留归属，明确标出被缩短的 `statement` 预览或 steering，并在读取 v0.1.2 的 Cursor 待取回记录时补齐归属。

## v0.1.2

- 修正 Engram 唤醒的上下文组装：原始消息现在按照原有角色与顺序作为真正的会话消息提交，不再压成 `<original_context>` 引用文本。
- 在原始会话之后加入明确的唤醒接缝和主线程观察切片；多轮陪伴继续区分 Engram 自己的回应与主线程的外部消息。
- 澄清“模型调用不保存状态”只表示不依赖提供方远程线程，不表示丢失或替换 Engram 上下文。
- `statement` 降为可选目录备注；真实模型调用不再用它制造 Engram 身份。

## v0.1.1

- Journal 超过 128 MiB 时明确失败，不再把截断的事件流当作完整历史。
- Linux、macOS 和 Windows 使用操作系统文件锁协调 MCP 与 Hook 进程；同一数据目录的运行时事务按顺序执行。
- MCP 请求 Schema 与 Go JSON 请求类型由自动一致性测试约束，并补齐时间戳、宿主回合与重试字段。
- CI 同时验证最低 Go 1.22 与当前发布工具链，并在当前工具链执行并发竞争检测。

## v0.1.0

- Engram 原始上下文全文与本地只追加 Journal。
- MCP 具名召唤、多轮唤醒、观察与释放。
- Codex、Claude Code、Cursor 和 Pi 接入模板。
- 可选 Hook 自动守护。
- 分开的英文与简体中文文档版本。
